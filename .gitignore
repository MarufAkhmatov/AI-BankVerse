# === Secrets ===
.env
.env.*
!.env.example
*.key
*.pem
*credentials*.json

# === Node / TypeScript (core/, prototype/web-client/) ===
node_modules/
dist/
build/
*.tsbuildinfo
coverage/
.vite/
.turbo/

# === Unreal Engine (prototype/unreal/, future) ===
Binaries/
Intermediate/
DerivedDataCache/
Saved/
Build/
*.VC.db
*.opensdf
*.sdf
*.sln
*.suo
*.xcodeproj
*.xcworkspace
.vs/

# === OS / editor ===
.DS_Store
Thumbs.db
.idea/
*.log

# === Raw supplied 3D source assets (multi-GB, unoptimized) ===
# Optimized/decimated copies actually used by the app live in
# prototype/web-client/public/models/ and ARE tracked (via Git LFS).
# Note: .gitignore patterns are literal, not shell-quoted — no quotes around the spaces.
prototype/the bank/
prototype/workers and clients and police/
